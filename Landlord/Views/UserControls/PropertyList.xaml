<UserControl
    x:Class="Landlord.Views.UserControls.PropertyList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vOs="clr-namespace:Landlord.VOs"
    d:DesignHeight="300"
    d:DesignWidth="300"
    DataContext="{Binding PropertyViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="PropertyItemTemplate" DataType="vOs:PropertyVo">
            <StackPanel Margin="5">
                <TextBlock FontWeight="Bold" Text="{Binding FullAddress}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Style="{StaticResource CaptionStyle}"
                Text="Filter" />
            <TextBox
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                Style="{StaticResource TextBoxStyle}"
                Text="{Binding FilterValue, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
        <ListView
            Grid.Row="1"
            Style="{StaticResource OverallStyle}"
            ItemTemplate="{StaticResource PropertyItemTemplate}"
            ItemsSource="{Binding Properties}"
            SelectedItem="{Binding PropertyVo}" />
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="{Binding ElementName=DeleteButton, Path=ActualWidth}"
                Style="{StaticResource OverallStyle}"
                Command="{Binding AddPropertyCommand}"
                Content="Add" />
            <Button
                Width="{Binding ElementName=DeleteButton, Path=ActualWidth}"
                Style="{StaticResource OverallStyle}"
                Command="{Binding EditPropertyCommand}"
                Content="Edit" />
            <Button
                x:Name="DeleteButton"
                Style="{StaticResource OverallStyle}"
                Command="{Binding DeletePropertyCommand}"
                Content="Delete" />
        </StackPanel>
    </Grid>
</UserControl>